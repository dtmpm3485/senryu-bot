PyPI Pending Trusted Publisher settings

PyPI project name: senryu-bot
Owner: dtmpm3485
Repository: senryu-bot
Workflow name: release.yml
Environment name: (leave blank)

After the publisher is registered, the release workflow builds Windows/Linux/macOS wheels plus an sdist and publishes them to PyPI via OIDC. No PyPI API token is required.
