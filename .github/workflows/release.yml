name: release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"
    branches:
      - "release/**"

jobs:
  build-desktop:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x64
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            manylinux: "2_28"
          - name: Linux ARM64
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            manylinux: "2_28"
          - name: Windows x64
            runner: windows-latest
            target: x86_64-pc-windows-msvc
            manylinux: "off"
          - name: Windows ARM64
            runner: windows-11-arm
            target: aarch64-pc-windows-msvc
            manylinux: "off"
          - name: macOS Intel
            runner: macos-15-intel
            target: x86_64-apple-darwin
            manylinux: "off"
          - name: macOS Apple Silicon
            runner: macos-latest
            target: aarch64-apple-darwin
            manylinux: "off"
    steps:
      - uses: actions/checkout@v4
      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          manylinux: ${{ matrix.manylinux }}
          args: --release --out dist --compatibility pypi
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.target }}
          path: dist/*

  build-android:
    name: Build Android ${{ matrix.target }}
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        target:
          - aarch64-linux-android
          - x86_64-linux-android
    env:
      ANDROID_API_LEVEL: "24"
      ANDROID_PYTHON_VERSION: "3.13.15"
    steps:
      - uses: actions/checkout@v4
      - name: Build Android wheel
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          manylinux: "off"
          args: --release --out dist --compatibility pypi
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-android-${{ matrix.target }}
          path: dist/*

  build-ios:
    name: Build iOS ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: device-arm64-and-simulator-arm64
            runner: macos-latest
            archs: "arm64_iphoneos arm64_iphonesimulator"
            rust_targets: "aarch64-apple-ios aarch64-apple-ios-sim"
          - name: simulator-x86_64
            runner: macos-15-intel
            archs: "x86_64_iphonesimulator"
            rust_targets: "x86_64-apple-ios"
    env:
      CIBW_BUILD: "cp313-*"
      CIBW_ARCHS_IOS: ${{ matrix.archs }}
      CIBW_XBUILD_TOOLS_IOS: "rustc cargo rustup cc ar"
      IPHONEOS_DEPLOYMENT_TARGET: "13.0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: dtolnay/rust-toolchain@stable
      - name: Install iOS Rust targets
        shell: bash
        run: rustup target add ${{ matrix.rust_targets }}
      - name: Install cibuildwheel
        run: python -m pip install "cibuildwheel==4.2.0"
      - name: Build iOS wheels
        run: python -m cibuildwheel --platform ios --output-dir dist
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-ios-${{ strategy.job-index }}
          path: dist/*

  build-sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: dtolnay/rust-toolchain@stable
      - name: Install maturin
        run: python -m pip install --upgrade pip maturin
      - name: Check Rust
        run: cargo check --all-targets
      - name: Run Rust tests
        run: cargo test --lib
      - name: Build sdist
        run: maturin sdist --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*

  publish:
    name: Publish to PyPI
    needs: [build-desktop, build-android, build-ios, build-sdist]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: "*"
          path: dist
          merge-multiple: true
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
